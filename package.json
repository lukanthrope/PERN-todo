{
  "name": "pern-todo",
  "version": "1.0.0",
  "description": "todo-list written with PERN stack with Redux & Immutable",
  "main": "index.js",
  "scripts": {
    "client": "cd client && npm start",
    "server": "cd server && nodemon index.js",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "lukanthrope",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^5.2.0",
    "nodemon": "^2.0.3"
  }
}
